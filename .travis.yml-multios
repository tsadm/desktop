language: python

matrix:
  include:
    - os: linux
      python: 3.5.1
    - os: osx

install:
  - ./.travis/install.sh

script:
  - ./.travis/test.sh

notifications:
  irc:
    - irc.freenode.org#tsadm
