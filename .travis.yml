sudo: false

dist: trusty

language: python

python:
  - 3.4.4
  - 3.5.1

build:
  - make build

install:
  - pip install coverage
  - pip install codecov
  - make install

script:
  - coverage run --source='.' test.py

after_success:
  - coverage report
  - codecov

notifications:
  irc:
    - irc.freenode.org#tsadm
